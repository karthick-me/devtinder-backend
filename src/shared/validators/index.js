const {
    validateRegisterUser,
    validateUpdateProfile,
    validateUserById,
} = require("./user.validator");

module.exports = {
    validateRegisterUser,
    validateUpdateProfile,
    validateUserById,
};
